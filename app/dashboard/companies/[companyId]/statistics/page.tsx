'use client';

export default function StatisticsPage({ params }: { params: { companyId: string } }) {
    return (
        <div>
            <h1 className="text-2xl font-bold mb-4">Estadísticas</h1>
            {/* Aquí irán los gráficos y estadísticas */}
            <p className="text-muted-foreground">Dashboard de estadísticas en desarrollo...</p>
        </div>
    );
}