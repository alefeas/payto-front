'use client';

export default function PaymentProjectionPage({ params }: { params: { companyId: string } }) {
    return (
        <div>
            <h1 className="text-2xl font-bold mb-4">Proyección de Pagos</h1>
            {/* Aquí irá la proyección de pagos */}
            <p className="text-muted-foreground">Proyección de pagos en desarrollo...</p>
        </div>
    );
}