'use client';

export default function ProvidersPage({ params }: { params: { companyId: string } }) {
    return (
        <div>
            <h1 className="text-2xl font-bold mb-4">Proveedores</h1>
            {/* Aquí irá la gestión de proveedores */}
            <p className="text-muted-foreground">Gestión de proveedores en desarrollo...</p>
        </div>
    );
}